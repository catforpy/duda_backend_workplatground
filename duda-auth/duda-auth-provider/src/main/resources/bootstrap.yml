# Bootstrap 配置 - 认证服务
# 此文件在 application.yml 之前加载，用于连接 Nacos

spring:
  application:
    name: duda-auth-provider

  cloud:
    nacos:
      # 🔐 Nacos 认证配置（必须）
      username: ${NACOS_USERNAME:nacos}
      password: ${NACOS_PASSWORD:nacos}

      # 服务发现配置
      discovery:
        server-addr: ${NACOS_HOST:120.26.170.213}:${NACOS_PORT:8848}
        namespace: ${NACOS_NAMESPACE:duda-dev}
        group: AUTH_GROUP  # 🔑 认证服务使用 AUTH_GROUP
        metadata:
          management.context-path: /actuator
          version: ${PROJECT_VERSION:1.0.0}

      # 配置中心配置
      config:
        server-addr: ${NACOS_HOST:120.26.170.213}:${NACOS_PORT:8848}
        namespace: ${NACOS_NAMESPACE:duda-dev}
        group: AUTH_GROUP  # 🔑 认证服务使用 AUTH_GROUP
        file-extension: yml
        refresh-enabled: true

        # 共享配置
        shared-configs:
          # 公共配置（从 COMMON_GROUP 引入）
          - data-id: common-${spring.profiles.active:dev}.yml
            group: COMMON_GROUP  # 🔑 引用公共配置 Group
            refresh: true

# 配置文件激活
spring:
  profiles:
    active: ${SPRING_PROFILES_ACTIVE:dev}
