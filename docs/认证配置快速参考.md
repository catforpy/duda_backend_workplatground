# 认证配置快速参考

## 🔑 认证信息汇总

### 服务器
- **地址**: 120.26.170.213
- **状态**: ✅ 已开启认证

---

## 📋 服务认证清单

### 1. Nacos

| 项目 | 值 |
|------|-----|
| 控制台 | http://120.26.170.213:8848/nacos |
| 用户名 | nacos |
| 密码 | nacos |
| 命名空间 | public |
| 分组 | DEFAULT_GROUP |

**配置方式：**
```yaml
spring:
  cloud:
    nacos:
      username: nacos        # 🔐 必须配置
      password: nacos        # 🔐 必须配置
```

**访问方式：**
```bash
# 浏览器访问
open http://120.26.170.213:8848/nacos

# 命令行测试
curl -u nacos:nacos http://120.26.170.213:8848/nacos/v1/console/health/readiness
```

---

### 2. Redis

| 项目 | 值 |
|------|-----|
| 地址 | 120.26.170.213:6379 |
| 密码 | duda2024 |
| 数据库 | 0 |

**配置方式：**
```yaml
spring:
  data:
    redis:
      host: 120.26.170.213
      port: 6379
      password: duda2024    # 🔐 必须配置
```

**连接方式：**
```bash
# 命令行连接
redis-cli -h 120.26.170.213 -p 6379 -a duda2024

# 测试
redis-cli -h 120.26.170.213 -p 6379 -a duda2024 ping
```

---

### 3. MySQL

| 项目 | 值 |
|------|-----|
| 地址 | 120.26.170.213:3306 |
| 用户名 | root |
| 密码 | duda2024 |

**配置方式：**
```yaml
spring:
  datasource:
    url: jdbc:mysql://120.26.170.213:3306/duda_${module}?...
    username: root
    password: duda2024     # 🔐 必须配置
```

**连接方式：**
```bash
# 命令行连接
mysql -h 120.26.170.213 -P 3306 -u root -pduda2024

# 连接特定数据库
mysql -h 120.26.170.213 -P 3306 -u root -pduda2024 duda_auth
```

---

## 🚀 快速配置模板

### bootstrap.yml

```yaml
spring:
  application:
    name: your-service-name
  cloud:
    nacos:
      username: nacos        # 🔐 认证
      password: nacos        # 🔐 认证
      discovery:
        server-addr: 120.26.170.213:8848
        namespace: public
        group: DEFAULT_GROUP
      config:
        server-addr: 120.26.170.213:8848
        namespace: public
        group: DEFAULT_GROUP
        file-extension: yml
```

### application.yml

```yaml
spring:
  data:
    redis:
      host: 120.26.170.213
      port: 6379
      password: duda2024    # 🔐 Redis 密码

  datasource:
    url: jdbc:mysql://120.26.170.213:3306/duda_${module}?...
    username: root
    password: duda2024     # 🔐 MySQL 密码
```

---

## 🧪 测试连接

```bash
# 运行测试脚本
./scripts/test-connection.sh
```

**预期结果：**
```
1️⃣  测试 Nacos...
   ✅ Nacos 连接成功
   📍 控制台: http://120.26.170.213:8848/nacos
   👤 账号: nacos / nacos

2️⃣  测试 Redis...
   ✅ Redis 连接成功

3️⃣  测试 MySQL...
   ✅ MySQL 端口可访问
```

---

## ⚠️ 重要提示

### 认证配置必须项

**所有服务必须配置以下认证信息：**

1. **Nacos**: `username: nacos`, `password: nacos`
2. **Redis**: `password: duda2024`
3. **MySQL**: `username: root`, `password: duda2024`

### 数据库命名规则

- 按模块分库：`duda_auth`, `duda_user`, `duda_order` 等
- 示例 URL: `jdbc:mysql://120.26.170.213:3306/duda_auth?...`

### 环境变量

使用环境变量管理敏感信息（推荐）：

```bash
# 复制模板
cp .env.template .env

# 编辑配置
vim .env

# 加载环境变量
source .env
```

---

## 📚 相关文档

- [服务器基础设施配置-认证版](./服务器基础设施配置-认证版.md) - 完整配置说明
- [如何连接到服务器](./如何连接到服务器.md) - 连接步骤详解
- [bootstrap配置模板](./bootstrap配置模板.yml) - 完整配置模板

---

**更新时间**: 2026-03-10
**状态**: ✅ 认证已开启
