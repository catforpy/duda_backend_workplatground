# 🔐 服务器基础设施配置（已开启认证）

## 🖥️ 服务器信息

**服务器地址**: 120.26.170.213
**创建时间**: 2026-03-10
**安全状态**: ✅ 已开启认证

---

## 🔐 配置清单（认证已开启）

### 1️⃣ Nacos 配置

| 配置项 | 值 |
|--------|-----|
| 服务地址 | 120.26.170.213:8848 |
| 控制台 | http://120.26.170.213:8848/nacos |
| 用户名 | nacos |
| 密码 | nacos |
| 命名空间 | public |
| Group | DEFAULT_GROUP |

**用途：**
- 服务注册与发现
- 配置中心（已开启认证）
- 动态配置管理

**连接配置：**
```yaml
spring:
  cloud:
    nacos:
      server-addr: 120.26.170.213:8848
      username: nacos        # 🔐 必须配置
      password: nacos        # 🔐 必须配置
      discovery:
        namespace: public
        group: DEFAULT_GROUP
      config:
        namespace: public
        group: DEFAULT_GROUP
        file-extension: yml
```

**访问 Nacos 控制台：**
```bash
# 浏览器访问
http://120.26.170.213:8848/nacos

# 登录
用户名: nacos
密码: nacos
```

---

### 2️⃣ Redis 配置

| 配置项 | 值 |
|--------|-----|
| 地址 | 120.26.170.213:6379 |
| 密码 | duda2024 |
| 数据库 | 0 |

**用途：**
- 分布式缓存
- 会话存储
- 分布式锁

**连接配置：**
```yaml
spring:
  data:
    redis:
      host: 120.26.170.213
      port: 6379
      password: duda2024    # 🔐 Redis 密码已配置
      database: 0
      timeout: 10s
      lettuce:
        pool:
          max-active: 20
          max-idle: 10
          min-idle: 5
          max-wait: 2s
```

**测试连接：**
```bash
# 命令行连接
redis-cli -h 120.26.170.213 -p 6379 -a duda2024

# 测试命令
redis-cli -h 120.26.170.213 -p 6379 -a duda2024 ping
redis-cli -h 120.26.170.213 -p 6379 -a duda2024 info server
```

---

### 3️⃣ MySQL 配置

| 配置项 | 值 |
|--------|-----|
| 地址 | 120.26.170.213:3306 |
| 用户名 | root |
| 密码 | duda2024 |

**数据库名称规则：**
- 按模块分库：`duda_auth`、`duda_user`、`duda_order` 等

**用途：**
- 关系型数据库
- 业务数据存储

**连接配置：**
```yaml
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://120.26.170.213:3306/duda_${module}?useUnicode=true&characterEncoding=utf8mb4&serverTimezone=Asia/Shanghai&useSSL=false
    username: root
    password: duda2024    # 🔐 MySQL 密码
    druid:
      initial-size: 5
      min-idle: 5
      max-active: 20
      max-wait: 60000
```

**创建数据库示例：**
```sql
-- 认证服务数据库
CREATE DATABASE IF NOT EXISTS duda_auth
DEFAULT CHARACTER SET utf8mb4
COLLATE utf8mb4_unicode_ci;

-- 用户服务数据库
CREATE DATABASE IF NOT EXISTS duda_user
DEFAULT CHARACTER SET utf8mb4
COLLATE utf8mb4_unicode_ci;

-- 订单服务数据库
CREATE DATABASE IF NOT EXISTS duda_order
DEFAULT CHARACTER SET utf8mb4
COLLATE utf8mb4_unicode_ci;

-- 授权
GRANT ALL PRIVILEGES ON duda_auth.* TO 'root'@'%';
GRANT ALL PRIVILEGES ON duda_user.* TO 'root'@'%';
GRANT ALL PRIVILEGES ON duda_order.* TO 'root'@'%';
FLUSH PRIVILEGES;
```

**连接测试：**
```bash
# 命令行连接
mysql -h 120.26.170.213 -P 3306 -u root -pduda2024

# 连接特定数据库
mysql -h 120.26.170.213 -P 3306 -u root -pduda2024 duda_auth
```

---

### 4️⃣ RocketMQ 配置

| 配置项 | 值 |
|--------|-----|
| NameServer | 120.26.170.213:9876 |

**用途：**
- 异步消息处理
- 服务解耦
- 流量削峰

**连接配置：**
```yaml
rocketmq:
  name-server: 120.26.170.213:9876
  producer:
    group: duda-producer-group
    send-message-timeout: 3000
    retry-times-when-send-failed: 2
  consumer:
    group: duda-consumer-group
```

---

## 📊 服务运行状态

| 服务 | 状态 | 内存使用 | 访问地址 |
|------|------|----------|----------|
| duda-nacos | ✅ | ~600MB / 1GB | http://120.26.170.213:8848/nacos |
| duda-redis | ✅ | ~7MB / 512MB | 120.26.170.213:6379 |
| duda-mysql | ✅ | ~400MB / 1GB | 120.26.170.213:3306 |
| rocketmq-namesrv | ✅ | ~173MB | 120.26.170.213:9876 |
| rocketmq-broker | ✅ | ~2GB | 120.26.170.213:10911 |

**总计内存**: ~3.2GB

---

## 🎯 完整配置示例

### application-dev.yml（认证已开启）

```yaml
server:
  port: 8081

spring:
  application:
    name: duda-auth-provider

  cloud:
    nacos:
      server-addr: 120.26.170.213:8848
      username: nacos        # 🔐 必须配置
      password: nacos        # 🔐 必须配置
      discovery:
        namespace: public
        group: DEFAULT_GROUP
      config:
        namespace: public
        group: DEFAULT_GROUP
        file-extension: yml
        refresh-enabled: true

  data:
    redis:
      host: 120.26.170.213
      port: 6379
      password: duda2024
      database: 0
      timeout: 10s
      lettuce:
        pool:
          max-active: 20
          max-idle: 10
          min-idle: 5
          max-wait: 2s

  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://120.26.170.213:3306/duda_auth?useUnicode=true&characterEncoding=utf8mb4&serverTimezone=Asia/Shanghai&useSSL=false
    username: root
    password: duda2024
    druid:
      initial-size: 5
      min-idle: 5
      max-active: 20
      max-wait: 60000

  rocketmq:
    name-server: 120.26.170.213:9876
    producer:
      group: duda-producer-group

# MyBatis-Plus
mybatis-plus:
  configuration:
    map-underscore-to-camel-case: true
    log-impl: org.apache.ibatis.logging.slf4j.Slf4jImpl
  global-config:
    db-config:
      id-type: auto
      logic-delete-field: deleted
      logic-delete-value: 1
      logic-not-delete-value: 0

# Actuator
management:
  endpoints:
    web:
      exposure:
        include: '*'
```

---

## 🔐 安全配置

### 1. Nacos 认证

**已开启认证：**
- ✅ 用户名/密码认证
- ✅ 命名空间隔离
- ✅ 权限控制

**访问方式：**
```bash
# Nacos 控制台
http://120.26.170.213:8848/nacos

# API 访问（需要认证）
curl -u nacos:nacos http://120.26.170.213:8848/nacos/v1/cs/configs
```

### 2. Redis 密码

**已配置密码：**
- ✅ 密码: `duda2024`
- ⚠️ 建议定期更换

**密码修改：**
```bash
# 连接 Redis
redis-cli -h 120.26.170.213 -p 6379 -a duda2024

# 修改密码
CONFIG SET requirepass your_new_password

# 验证
AUTH your_new_password

# 保存配置
CONFIG REWRITE
```

### 3. MySQL 安全

**已配置：**
- ✅ root 账户密码
- ⚠️ 建议创建专用应用账号

**创建应用账号：**
```sql
-- 为每个服务创建独立账号
CREATE USER 'duda_auth'@'%' IDENTIFIED BY 'strong_password_here';
GRANT ALL PRIVILEGES ON duda_auth.* TO 'duda_auth'@'%';

CREATE USER 'duda_user'@'%' IDENTIFIED BY 'strong_password_here';
GRANT ALL PRIVILEGES ON duda_user.* TO 'duda_user'@'%';

FLUSH PRIVILEGES;
```

---

## 🧪 连接测试脚本

```bash
#!/bin/bash

echo "=== DudaNexus 服务器连接测试 ==="
echo ""

# 1. 测试 Nacos
echo "1️⃣ 测试 Nacos 认证..."
curl -u nacos:nacos \
  http://120.26.170.213:8848/nacos/v1/console/health/readiness

echo ""

# 2. 测试 Redis
echo "2️⃣ 测试 Redis..."
redis-cli -h 120.26.170.213 -p 6379 -a duda2024 ping

echo ""

# 3. 测试 MySQL
echo "3️⃣ 测试 MySQL..."
mysql -h 120.26.170.213 -P 3306 -u root -pduda2024 -e "SELECT 1"

echo ""
echo "=== 测试完成 ==="
```

---

## 📝 快速配置模板

### bootstrap.yml

```yaml
spring:
  application:
    name: ${SERVICE_NAME:duda-auth-provider}
  cloud:
    nacos:
      server-addr: 120.26.170.213:8848
      username: nacos        # 🔐 认证必需
      password: nacos        # 🔐 认证必需
      discovery:
        namespace: public
        group: DEFAULT_GROUP
      config:
        namespace: public
        group: DEFAULT_GROUP
        file-extension: yml
```

### application-dev.yml

```yaml
spring:
  data:
    redis:
      host: 120.26.170.213
      port: 6379
      password: duda2024  # 🔐 密码已配置

  datasource:
    url: jdbc:mysql://120.26.170.213:3306/duda_${module}?...
    username: root
    password: duda2024    # 🔐 密码已配置

  rocketmq:
    name-server: 120.26.170.213:9876
```

---

## ⚠️ 重要提示

### 1. 认证配置

**Nacos 已开启认证，必须配置：**
```yaml
spring:
  cloud:
    nacos:
      username: nacos    # 🔐 必须配置
      password: nacos    # 🔐 必须配置
```

### 2. 数据库命名

**按模块分库：**
- `duda_auth` - 认证服务
- `duda_user` - 用户服务
- `duda_order` - 订单服务
- 等等...

**示例 URL：**
```
jdbc:mysql://120.26.170.213:3306/duda_auth?...
jdbc:mysql://120.26.170.213:3306/duda_user?...
```

### 3. 密码安全

**当前密码：**
- Nacos: `nacos` / `nacos`
- Redis: `duda2024`
- MySQL: `duda2024`

**建议：**
- ⚠️ 生产环境必须修改所有默认密码
- ⚠️ 定期更换密码
- ⚠️ 使用强密码（大小写字母+数字+特殊字符）

---

## 🔗 访问地址汇总

| 服务 | 地址 | 说明 |
|------|------|------|
| Nacos 控制台 | http://120.26.170.213:8848/nacos | 需要登录 |
| Nacos API | http://120.26.170.213:8848/nacos | API 接口 |
| Redis CLI | `redis-cli -h 120.26.170.213 -p 6379 -a duda2024` | 命令行工具 |
| MySQL CLI | `mysql -h 120.26.170.213 -P 3306 -u root -pduda2024` | 命令行工具 |

---

## 🚀 快速开始

### 1. 测试连接

```bash
# 运行测试脚本
cd /Volumes/DudaDate/DudaNexus
./scripts/test-connection.sh
```

### 2. 访问 Nacos

```bash
# 浏览器访问
open http://120.26.170.213:8848/nacos

# 或使用命令行
curl -u nacos:nacos http://120.26.170.213:8848/nacos
```

### 3. 配置项目

复制配置到你的项目：

```yaml
# application-dev.yml
spring:
  cloud:
    nacos:
      server-addr: 120.26.170.213:8848
      username: nacos
      password: nacos
```

---

## 📚 相关文档

- [如何连接到服务器](./如何连接到服务器.md) - 详细的连接步骤
- [接口-实现分离代码结构示例](./接口-实现分离代码结构示例.md) - 代码示例

---

**更新时间**: 2026-03-10
**状态**: ✅ 认证已开启，所有服务正常运行
