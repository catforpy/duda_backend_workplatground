# 服务器基础设施配置

## 🖥️ 服务器信息

**服务器地址**: 120.26.170.213
**创建时间**: 2026-03-10

---

## 📋 基础设施服务列表

### 1. Nacos（注册中心 & 配置中心）

| 配置项 | 值 |
|--------|-----|
| 服务名 | duda-nacos |
| 内存使用 | 608MB / 1GB |
| 状态 | ✅ 运行中 |
| 访问地址 | http://120.26.170.213:8848/nacos |
| 默认账号 | nacos / nacos |

**用途：**
- 服务注册与发现
- 配置中心
- 动态配置管理

**连接配置：**
```yaml
spring:
  cloud:
    nacos:
      discovery:
        server-addr: 120.26.170.213:8848
        namespace: duda-dev
      config:
        server-addr: 120.26.170.213:8848
        file-extension: yml
        namespace: duda-dev
```

---

### 2. Redis（缓存）

| 配置项 | 值 |
|--------|-----|
| 服务名 | duda-redis |
| 内存使用 | 7MB / 512MB |
| 状态 | ✅ 运行中 |
| 访问地址 | 120.26.170.213:6379 |
| 密码 | (需要记录) |

**用途：**
- 分布式缓存
- 会话存储
- 分布式锁
- 消息队列

**连接配置：**
```yaml
spring:
  redis:
    host: 120.26.170.213
    port: 6379
    password: your_password
    database: 0
    jedis:
      pool:
        max-active: 8
        max-idle: 8
        min-idle: 0
```

**Jedis 连接：**
```bash
# 命令行连接
redis-cli -h 120.26.170.213 -p 6379 -a your_password

# 测试连接
redis-cli -h 120.26.170.213 -p 6379 ping
```

---

### 3. MySQL（数据库）

| 配置项 | 值 |
|--------|-----|
| 服务名 | duda-mysql |
| 内存使用 | 405MB / 1GB |
| 状态 | ✅ 运行中 |
| 访问地址 | 120.26.170.213:3306 |
| 端口 | 3306 |

**用途：**
- 关系型数据库
- 业务数据存储

**连接配置：**
```yaml
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://120.26.170.213:3306/duda_dev?useUnicode=true&characterEncoding=utf8mb4&useSSL=false&serverTimezone=Asia/Shanghai
    username: duda_dev
    password: your_password
```

**数据库初始化：**
```sql
-- 创建数据库
CREATE DATABASE IF NOT EXISTS duda_dev DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;

-- 创建用户
CREATE USER IF NOT EXISTS 'duda_dev'@'%' IDENTIFIED BY 'your_password';

-- 授权
GRANT ALL PRIVILEGES ON duda_dev.* TO 'duda_dev'@'%';

-- 刷新权限
FLUSH PRIVILEGES;
```

**连接测试：**
```bash
# 命令行连接
mysql -h 120.26.170.213 -P 3306 -u duda_dev -p

# 或使用本地端口转发
mysql -h 127.0.0.1 -P 3306 -u root -p
```

---

### 4. RocketMQ NameServer

| 配置项 | 值 |
|--------|-----|
| 服务名 | rocketmq-namesrv |
| 内存使用 | 173MB |
| 状态 | ✅ 运行中 |
| 访问地址 | 120.26.170.213:9876 |

**用途：**
- RocketMQ 路由信息管理
- 服务发现

**连接配置：**
```yaml
rocketmq:
  name-server: 120.26.170.213:9876
  producer:
    group: duda-producer-group
    send-message-timeout: 3000
    retry-times-when-send-failed: 2
  consumer:
    group: duda-consumer-group
```

---

### 5. RocketMQ Broker

| 配置项 | 值 |
|--------|-----|
| 服务名 | rocketmq-broker |
| 内存使用 | 2.01GB |
| 状态 | ⚠️ 内存使用较高 |
| 访问地址 | 120.26.170.213:10911 |

**用途：**
- 消息队列 Broker
- 消息存储和转发

**内存优化建议：**
⚠️ Broker 内存使用 2.01GB，建议优化：

1. **调整 JVM 堆内存**
   ```bash
   # 编辑 runbroker.sh
   JAVA_OPT="${JAVA_OPT} -Xms1g -Xmx1g"
   ```

2. **清理过期消息**
   ```bash
   # 默认保留 72 小时
   # 可调整为 24 小时
   ```

3. **监控内存使用**
   ```bash
   # 查看内存监控
   sh mqadmin clusterList -n 120.26.170.213:9876
   ```

---

## 🔗 连接信息总览

| 服务 | 地址 | 端口 | 协议 | 说明 |
|------|------|------|------|------|
| Nacos | 120.26.170.213 | 8848 | HTTP | 服务注册/配置中心 |
| Redis | 120.26.170.213 | 6379 | Redis | 缓存 |
| MySQL | 120.26.170.213 | 3306 | MySQL | 数据库 |
| RocketMQ NameServer | 120.26.170.213 | 9876 | TCP | NameServer |
| RocketMQ Broker | 120.26.170.213 | 10911 | TCP | Broker |

---

## 📊 资源使用情况

| 服务类型 | 内存使用 | 内存限制 | CPU使用 | 状态 |
|---------|---------|---------|---------|------|
| Nacos | 608MB | 1GB | - | ✅ 正常 |
| Redis | 7MB | 512MB | - | ✅ 正常 |
| MySQL | 405MB | 1GB | - | ✅ 正常 |
| RocketMQ NameServer | 173MB | - | - | ✅ 正常 |
| RocketMQ Broker | 2.01GB | - | - | ⚠️ 需优化 |

**总计内存**: ~3.2GB

---

## ⚙️ Bootstrap 配置

在 `src/main/resources/bootstrap.yml` 中配置：

```yaml
spring:
  application:
    name: your-service-name
  cloud:
    nacos:
      discovery:
        server-addr: 120.26.170.213:8848
        namespace: duda-dev
        group: DUDA_DEFAULT_GROUP
      config:
        server-addr: 120.26.170.213:8848
        namespace: duda-dev
        group: DUDA_DEFAULT_GROUP
        file-extension: yml
```

---

## 🔐 安全建议

### 1. 修改默认密码

**Nacos:**
```bash
# 访问 Nacos 控制台
http://120.26.170.213:8848/nacos

# 修改默认密码 nacos/nacos
```

**MySQL:**
- ✅ 已创建专用用户 `duda_dev`
- ⚠️ 记得修改密码

**Redis:**
- ⚠️ 建议设置密码
- 在配置文件中启用密码验证

### 2. 防火墙配置

```bash
# 开放端口
8848  # Nacos
3306  # MySQL
6379  # Redis
9876  # RocketMQ NameServer
10911 # RocketMQ Broker
10909 # RocketMQ Console
```

### 3. 备份策略

**MySQL 备份:**
```bash
# 每天备份
0 2 * * * mysqldump -h 120.26.170.213 -u duda_dev -p'duda_dev' --all-databases > /backup/duda_$(date +\%Y\%m\%d).sql

# 保留 7 天
find /backup -name "duda_*.sql" -mtime +7 -delete
```

**Redis 备份:**
```bash
# 开启 AOF 持久化
# 在 redis.conf 中配置
appendonly yes
appendfsync everysec
```

---

## 🚀 快速启动指南

### 本地开发连接

```bash
# 1. 测试 Nacos 连接
curl http://120.26.170.213:8848/nacos/v1/console/health/readiness

# 2. 测试 Redis 连接
redis-cli -h 120.26.170.213 -p 6379 ping

# 3. 测试 MySQL 连接
mysql -h 120.26.170.213 -P 3306 -u duda_dev -p
```

### IDEA 配置

1. **Nacos 配置**
   - 打开 Nacos 控制台：http://120.26.170.213:8848/nacos
   - 登录：nacos/nacos
   - 创建命名空间：`duda-dev`

2. **数据库连接**
   - Host: 120.26.170.213
   - Port: 3306
   - Database: duda_dev
   - User: duda_dev

---

## 📝 TODO

### 内存优化

- [ ] RocketMQ Broker 内存优化（当前 2.01GB → 目标 1GB）
- [ ] MySQL 添加慢查询监控
- [ ] Redis 设置最大内存限制

### 安全加固

- [ ] 修改 Nacos 默认密码
- [ ] 配置 Redis 密码
- [ ] 配置防火墙规则
- [ ] 启用 SSL 连接

### 监控告警

- [ ] 部署 Prometheus 监控
- [ ] 配置 Grafana 可视化
- [ ] 设置告警规则
- [ ] 配置日志收集

---

## 📞 联系信息

**服务器管理员**: [待填写]
**运维负责人**: [待填写]
**紧急联系**: [待填写]

---

**更新时间**: 2026-03-10
**记录人**: Claude (AI Assistant)
