# 配置文件部署指南

本文档详细说明 DudaNexus 项目所有配置文件的部署位置和创建步骤。

---

## 📋 配置文件清单

### 本地配置文件（项目代码中）

| 配置文件 | 位置 | 用途 |
|---------|------|------|
| bootstrap.yml | 每个服务的 src/main/resources/ | 连接 Nacos，必须配置 |
| logback-spring.xml | duda-common-core/src/main/resources/ | 日志配置（所有服务共享） |
| Dockerfile | 每个服务根目录 | Docker 镜像构建 |
| docker-compose.yml | 项目根目录 | 本地/测试环境服务编排 |
| .dockerignore | 项目根目录 | Docker 构建忽略文件 |

### Nacos 配置（远程配置中心）

| 配置文件 | 命名空间 | Group | 用途 |
|---------|---------|-------|------|
| common-dev.yml | duda-dev | DEFAULT_GROUP | 公共配置（所有服务共享） |
| duda-auth-provider-dev.yml | duda-dev | DEFAULT_GROUP | 认证服务配置 |
| duda-user-provider-dev.yml | duda-dev | DEFAULT_GROUP | 用户服务配置 |
| duda-gateway-dev.yml | duda-dev | DEFAULT_GROUP | 网关服务配置 |
| duda-search-provider-dev.yml | duda-dev | DEFAULT_GROUP | 搜索服务配置 |
| duda-content-provider-dev.yml | duda-dev | DEFAULT_GROUP | 内容服务配置 |
| duda-order-provider-dev.yml | duda-dev | DEFAULT_GROUP | 订单服务配置 |

---

## 🔧 部署步骤

### 第一步：创建 Nacos 配置

#### 1. 登录 Nacos 控制台

```
地址: http://120.26.170.213:8848/nacos
账号: nacos
密码: nacos
```

#### 2. 创建命名空间

1. 点击左侧「命名空间」
2. 点击「新建命名空间」
3. 创建以下命名空间：

| 命名空间 ID | 名称 | 描述 |
|-----------|------|------|
| duda-dev | 开发环境 | 开发测试环境 |

#### 3. 创建公共配置（common-dev.yml）

1. 切换到 `duda-dev` 命名空间
2. 点击「配置管理」→「配置列表」
3. 点击「+」创建配置
4. 填写以下信息：

```
Data ID: common-dev.yml
Group: DEFAULT_GROUP
配置格式: YAML
配置内容: [复制下面内容]
```

**配置内容：**

```yaml
# 公共配置 - 开发环境
spring:
  # Redis 配置
  data:
    redis:
      host: 120.26.170.213
      port: 6379
      password: duda2024
      database: 0
      timeout: 10s
      lettuce:
        pool:
          max-active: 20
          max-idle: 10
          min-idle: 5
          max-wait: 2s

  # RocketMQ 配置
  rocketmq:
    name-server: 120.26.170.213:9876
    producer:
      group: duda-producer-group
      send-message-timeout: 3000
      retry-times-when-send-failed: 2
    consumer:
      group: duda-consumer-group

# MyBatis-Plus 配置
mybatis-plus:
  configuration:
    map-underscore-to-camel-case: true
    log-impl: org.apache.ibatis.logging.slf4j.Slf4jImpl
  global-config:
    db-config:
      id-type: auto
      logic-delete-field: deleted
      logic-delete-value: 1
      logic-not-delete-value: 0

# Actuator 配置
management:
  endpoints:
    web:
      exposure:
        include: '*'
  endpoint:
    health:
      show-details: always

# 日志配置
logging:
  level:
    root: INFO
    com.duda: DEBUG
    com.alibaba.cloud.nacos: INFO
    org.springframework.cloud.gateway: INFO
  pattern:
    console: '%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level [%logger{50}] %msg%n'
```

#### 4. 创建服务配置

为每个服务创建独立配置，重复以下步骤：

**认证服务配置（duda-auth-provider-dev.yml）：**

```yaml
server:
  port: 8081

spring:
  application:
    name: duda-auth-provider

  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://120.26.170.213:3306/duda_auth?useUnicode=true&characterEncoding=utf8mb4&serverTimezone=Asia/Shanghai&useSSL=false&allowPublicKeyRetrieval=true
    username: root
    password: duda2024
    druid:
      initial-size: 5
      min-idle: 5
      max-active: 20
      max-wait: 60000

duda:
  auth:
    jwt:
      secret: duda-auth-secret-key-2026
      expiration: 7200
    token:
      header: Authorization
      prefix: Bearer
```

**用户服务配置（duda-user-provider-dev.yml）：**

```yaml
server:
  port: 8082

spring:
  application:
    name: duda-user-provider

  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://120.26.170.213:3306/duda_user?useUnicode=true&characterEncoding=utf8mb4&serverTimezone=Asia/Shanghai&useSSL=false&allowPublicKeyRetrieval=true
    username: root
    password: duda2024
    druid:
      initial-size: 5
      min-idle: 5
      max-active: 20
      max-wait: 60000

duda:
  user:
    default-avatar: https://duda.com/default-avatar.png
    nickname-prefix: 用户
```

**网关服务配置（duda-gateway-dev.yml）：**

```yaml
server:
  port: 8080

spring:
  application:
    name: duda-gateway

  cloud:
    gateway:
      discovery:
        locator:
          enabled: true
          lower-case-service-id: true
      routes:
        # 认证服务路由
        - id: duda-auth-provider
          uri: lb://duda-auth-provider
          predicates:
            - Path=/api/auth/**
          filters:
            - StripPrefix=2

        # 用户服务路由
        - id: duda-user-provider
          uri: lb://duda-user-provider
          predicates:
            - Path=/api/user/**
          filters:
            - StripPrefix=2

duda:
  gateway:
    cors:
      allowed-origins: "*"
      allowed-methods: "*"
      allowed-headers: "*"
    auth:
      skip-paths:
        - /api/auth/login
        - /api/auth/register
        - /api/auth/logout
```

---

### 第二步：本地开发配置

#### 1. 配置环境变量（可选）

```bash
# 复制环境变量模板
cp .env.template .env

# 编辑 .env 文件
vim .env
```

**.env 文件内容：**

```bash
# 数据库配置
DB_HOST=120.26.170.213
DB_PORT=3306
DB_USER=root
DB_PASSWORD=duda2024

# Redis 配置
REDIS_HOST=120.26.170.213
REDIS_PORT=6379
REDIS_PASSWORD=duda2024

# Nacos 配置
NACOS_HOST=120.26.170.213
NACOS_PORT=8848
NACOS_USERNAME=nacos
NACOS_PASSWORD=nacos
NACOS_NAMESPACE=duda-dev
NACOS_GROUP=DEFAULT_GROUP

# 服务端口配置
GATEWAY_PORT=8080
AUTH_SERVICE_PORT=8081
USER_SERVICE_PORT=8082

# 日志级别
LOG_LEVEL=DEBUG

# Spring Profile
SPRING_PROFILES_ACTIVE=dev
```

#### 2. 在 IDEA 中配置

```
Run → Edit Configurations
  → 选择你的 Spring Boot 应用
  → Environment
    → 添加环境变量（或加载 .env 文件）
```

---

### 第三步：创建数据库

#### 连接 MySQL 并创建数据库

```bash
# 连接 MySQL
mysql -h 120.26.170.213 -P 3306 -u root -pduda2024

# 创建数据库
CREATE DATABASE IF NOT EXISTS duda_auth
DEFAULT CHARACTER SET utf8mb4
COLLATE utf8mb4_unicode_ci;

CREATE DATABASE IF NOT EXISTS duda_user
DEFAULT CHARACTER SET utf8mb4
COLLATE utf8mb4_unicode_ci;

CREATE DATABASE IF NOT EXISTS duda_search
DEFAULT CHARACTER SET utf8mb4
COLLATE utf8mb4_unicode_ci;

CREATE DATABASE IF NOT EXISTS duda_content
DEFAULT CHARACTER SET utf8mb4
COLLATE utf8mb4_unicode_ci;

CREATE DATABASE IF NOT EXISTS duda_order
DEFAULT CHARACTER SET utf8mb4
COLLATE utf8mb4_unicode_ci;

# 验证
SHOW DATABASES;
```

---

### 第四步：启动服务

#### 方式一：IDEA 启动（本地开发）

1. 打开 `duda-auth-provider` 服务的启动类
2. 右键 → Run 'AuthProviderApplication'
3. 观察日志，确认连接到 Nacos 成功
4. 依次启动其他服务

#### 方式二：Docker Compose 启动（测试环境）

```bash
# 构建并启动所有服务
docker-compose up -d

# 查看日志
docker-compose logs -f

# 查看服务状态
docker-compose ps

# 停止服务
docker-compose down
```

---

## ✅ 验证部署

### 1. 检查 Nacos 服务注册

访问 Nacos 控制台：
```
http://120.26.170.213:8848/nacos
```

查看「服务管理」→「服务列表」，应该看到：
- duda-auth-provider
- duda-user-provider
- duda-gateway

### 2. 测试服务健康检查

```bash
# 认证服务
curl http://localhost:8081/actuator/health

# 用户服务
curl http://localhost:8082/actuator/health

# 网关服务
curl http://localhost:8080/actuator/health
```

### 3. 测试网关路由

```bash
# 通过网关访问认证服务
curl http://localhost:8080/api/auth/health

# 通过网关访问用户服务
curl http://localhost:8080/api/user/health
```

---

## 📝 配置文件优先级

Spring Boot 配置加载顺序（优先级从高到低）：

1. `bootstrap.yml`（项目代码中）
2. Nacos 配置中心（远程）
   - 服务配置（如 duda-auth-provider-dev.yml）
   - 共享配置（如 common-dev.yml）
3. `application.yml`（项目代码中）
4. 环境变量
5. 命令行参数

**最佳实践：**
- 认证信息 → `bootstrap.yml`（必须）
- 公共配置 → Nacos `common-dev.yml`
- 服务配置 → Nacos `{服务名}-dev.yml`
- 本地开发配置 → `application-local.yml`（不提交到 Git）

---

## 🔧 故障排查

### 问题 1：服务无法连接 Nacos

**症状：**
```
Request nacos server failed:`
```

**解决：**
1. 检查 `bootstrap.yml` 中 Nacos 认证配置是否正确
2. 确认 Nacos 服务是否运行：`curl http://120.26.170.213:8848/nacos`
3. 检查防火墙是否开放 8848 端口

### 问题 2：配置无法加载

**症状：**
```
config data not found`
```

**解决：**
1. 确认 Nacos 配置文件已创建
2. 检查命名空间是否正确（`duda-dev`）
3. 检查 Data ID 格式是否正确（`{服务名}-dev.yml`）

### 问题 3：数据库连接失败

**症状：**
```
Could not create connection to database server`
```

**解决：**
1. 确认数据库已创建
2. 检查数据库密码是否正确
3. 测试连接：`mysql -h 120.26.170.213 -u root -pduda2024`

---

## 📚 相关文档

- [Nacos配置创建指南.md](./Nacos配置创建指南.md) - Nacos 配置详细说明
- [服务器基础设施配置-认证版.md](./服务器基础设施配置-认证版.md) - 服务器配置
- [认证配置快速参考.md](./认证配置快速参考.md) - 认证信息汇总

---

**更新时间**: 2026-03-10
**适用版本**: DudaNexus 1.0.0
