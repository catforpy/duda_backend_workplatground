# 🎯 配置部署清单 - 你需要做的事

## ✅ 已完成的配置（我已经帮你创建）

### 1. 本地配置文件

这些文件已经在项目中创建好了：

| 文件 | 路径 | 说明 |
|------|------|------|
| ✅ bootstrap.yml | `duda-auth/duda-auth-provider/src/main/resources/bootstrap.yml` | 认证服务启动配置 |
| ✅ bootstrap.yml | `duda-user/duda-user-provider/src/main/resources/bootstrap.yml` | 用户服务启动配置 |
| ✅ bootstrap.yml | `duda-gateway/src/main/resources/bootstrap.yml` | 网关启动配置 |
| ✅ logback-spring.xml | `duda-common/duda-common-core/src/main/resources/logback-spring.xml` | 日志配置（所有服务共享） |
| ✅ Dockerfile | `duda-auth/duda-auth-provider/Dockerfile` | 认证服务镜像 |
| ✅ Dockerfile | `duda-user/duda-user-provider/Dockerfile` | 用户服务镜像 |
| ✅ Dockerfile | `duda-gateway/Dockerfile` | 网关镜像 |
| ✅ docker-compose.yml | 项目根目录 | 服务编排 |
| ✅ .dockerignore | 项目根目录 | Docker 构建优化 |

### 2. 文档

| 文档 | 路径 | 说明 |
|------|------|------|
| ✅ Nacos配置创建指南.md | `docs/Nacos配置创建指南.md` | Nacos 配置详细说明 |
| ✅ 配置文件部署指南.md | `docs/配置文件部署指南.md` | 完整部署步骤 |

---

## 📝 你需要做的配置

### 任务一：在 Nacos 创建配置 ⭐⭐⭐（最重要）

#### 步骤 1：登录 Nacos

```
地址: http://120.26.170.213:8848/nacos
账号: nacos
密码: nacos
```

#### 步骤 2：创建命名空间

1. 点击「命名空间」
2. 点击「新建命名空间」
3. 创建一个命名空间：

| 命名空间 ID | 命名空间名称 | 描述 |
|-----------|------------|------|
| `duda-dev` | 开发环境 | 开发测试环境 |

#### 步骤 3：创建配置文件

在 `duda-dev` 命名空间下，创建以下配置：

**🔹 配置 1: common-dev.yml（公共配置）**

```
Data ID: common-dev.yml
Group: DEFAULT_GROUP
配置格式: YAML
```

**配置内容：**

```yaml
# 公共配置 - 开发环境
spring:
  # Redis 配置
  data:
    redis:
      host: 120.26.170.213
      port: 6379
      password: duda2024
      database: 0
      timeout: 10s
      lettuce:
        pool:
          max-active: 20
          max-idle: 10
          min-idle: 5
          max-wait: 2s

  # RocketMQ 配置
  rocketmq:
    name-server: 120.26.170.213:9876
    producer:
      group: duda-producer-group
      send-message-timeout: 3000
      retry-times-when-send-failed: 2

# MyBatis-Plus 配置
mybatis-plus:
  configuration:
    map-underscore-to-camel-case: true
    log-impl: org.apache.ibatis.logging.slf4j.Slf4jImpl
  global-config:
    db-config:
      id-type: auto
      logic-delete-field: deleted
      logic-delete-value: 1
      logic-not-delete-value: 0

# Actuator 配置
management:
  endpoints:
    web:
      exposure:
        include: '*'
  endpoint:
    health:
      show-details: always

# 日志配置
logging:
  level:
    root: INFO
    com.duda: DEBUG
```

---

**🔹 配置 2: duda-auth-provider-dev.yml（认证服务）**

```
Data ID: duda-auth-provider-dev.yml
Group: DEFAULT_GROUP
配置格式: YAML
```

**配置内容：**

```yaml
server:
  port: 8081

spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://120.26.170.213:3306/duda_auth?useUnicode=true&characterEncoding=utf8mb4&serverTimezone=Asia/Shanghai&useSSL=false&allowPublicKeyRetrieval=true
    username: root
    password: duda2024
    druid:
      initial-size: 5
      min-idle: 5
      max-active: 20
      max-wait: 60000

duda:
  auth:
    jwt:
      secret: duda-auth-secret-key-2026
      expiration: 7200
    token:
      header: Authorization
      prefix: Bearer
```

---

**🔹 配置 3: duda-user-provider-dev.yml（用户服务）**

```
Data ID: duda-user-provider-dev.yml
Group: DEFAULT_GROUP
配置格式: YAML
```

**配置内容：**

```yaml
server:
  port: 8082

spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://120.26.170.213:3306/duda_user?useUnicode=true&characterEncoding=utf8mb4&serverTimezone=Asia/Shanghai&useSSL=false&allowPublicKeyRetrieval=true
    username: root
    password: duda2024
    druid:
      initial-size: 5
      min-idle: 5
      max-active: 20
      max-wait: 60000
```

---

**🔹 配置 4: duda-gateway-dev.yml（网关服务）**

```
Data ID: duda-gateway-dev.yml
Group: DEFAULT_GROUP
配置格式: YAML
```

**配置内容：**

```yaml
server:
  port: 8080

spring:
  cloud:
    gateway:
      discovery:
        locator:
          enabled: true
          lower-case-service-id: true
      routes:
        # 认证服务路由
        - id: duda-auth-provider
          uri: lb://duda-auth-provider
          predicates:
            - Path=/api/auth/**
          filters:
            - StripPrefix=2

        # 用户服务路由
        - id: duda-user-provider
          uri: lb://duda-user-provider
          predicates:
            - Path=/api/user/**
          filters:
            - StripPrefix=2

duda:
  gateway:
    cors:
      allowed-origins: "*"
      allowed-methods: "*"
      allowed-headers: "*"
```

---

### 任务二：创建数据库

连接 MySQL 并创建数据库：

```bash
# 连接
mysql -h 120.26.170.213 -P 3306 -u root -pduda2024

# 创建数据库
CREATE DATABASE IF NOT EXISTS duda_auth
DEFAULT CHARACTER SET utf8mb4
COLLATE utf8mb4_unicode_ci;

CREATE DATABASE IF NOT EXISTS duda_user
DEFAULT CHARACTER SET utf8mb4
COLLATE utf8mb4_unicode_ci;

CREATE DATABASE IF NOT EXISTS duda_search
DEFAULT CHARACTER SET utf8mb4
COLLATE utf8mb4_unicode_ci;

CREATE DATABASE IF NOT EXISTS duda_content
DEFAULT CHARACTER SET utf8mb4
COLLATE utf8mb4_unicode_ci;

CREATE DATABASE IF NOT EXISTS duda_order
DEFAULT CHARACTER SET utf8mb4
COLLATE utf8mb4_unicode_ci;
```

---

### 任务三：配置本地开发环境（可选）

#### 方式 1: 使用 .env 文件

```bash
# 复制模板
cp .env.template .env

# 编辑（已有默认值，可以不改）
vim .env
```

#### 方式 2: 在 IDEA 中配置

```
Run → Edit Configurations
  → 选择你的 Spring Boot 应用
  → Environment variables:
    NACOS_USERNAME=nacos
    NACOS_PASSWORD=nacos
    NACOS_NAMESPACE=duda-dev
```

---

## ✅ 验证步骤

### 1. 验证 Nacos 配置

访问 Nacos 控制台，在 `duda-dev` 命名空间下应该看到：

- ✅ common-dev.yml
- ✅ duda-auth-provider-dev.yml
- ✅ duda-user-provider-dev.yml
- ✅ duda-gateway-dev.yml

### 2. 启动服务测试

```bash
# 在 IDEA 中启动 duda-auth-provider
# 观察日志，应该看到：
# - 成功连接到 Nacos
# - 成功加载配置
# - 服务注册成功
```

### 3. 检查服务注册

在 Nacos 控制台「服务管理」→「服务列表」中应该看到：

- ✅ duda-auth-provider
- ✅ duda-user-provider
- ✅ duda-gateway

---

## 📊 配置文件总览

### 本地配置文件（已创建 ✅）

```
duda-auth/duda-auth-provider/
├── src/main/resources/
│   └── bootstrap.yml          ← Nacos 连接配置
├── Dockerfile                  ← Docker 镜像构建
└── pom.xml

duda-user/duda-user-provider/
├── src/main/resources/
│   └── bootstrap.yml          ← Nacos 连接配置
├── Dockerfile                  ← Docker 镜像构建
└── pom.xml

duda-gateway/
├── src/main/resources/
│   └── bootstrap.yml          ← Nacos 连接配置
├── Dockerfile                  ← Docker 镜像构建
└── pom.xml

duda-common/duda-common-core/
└── src/main/resources/
    └── logback-spring.xml     ← 日志配置（共享）
```

### Nacos 配置（需要你创建 ⭐）

在 `duda-dev` 命名空间下创建：

```
duda-dev 命名空间
├── common-dev.yml                        ← 公共配置
├── duda-auth-provider-dev.yml            ← 认证服务配置
├── duda-user-provider-dev.yml            ← 用户服务配置
└── duda-gateway-dev.yml                  ← 网关配置
```

---

## 🎯 总结

### 我帮你完成的：
1. ✅ 创建了所有 `bootstrap.yml`（包含 Nacos 认证配置）
2. ✅ 创建了 `logback-spring.xml`（日志配置）
3. ✅ 创建了所有 `Dockerfile`（Docker 镜像构建）
4. ✅ 创建了 `docker-compose.yml`（服务编排）
5. ✅ 创建了详细的文档和指南

### 你需要做的：
1. ⭐ **登录 Nacos 创建配置**（最重要！）
   - 创建 `duda-dev` 命名空间
   - 创建 4 个配置文件（见上面的详细内容）
2. ⭐ **创建数据库**
   - 创建 5 个数据库（duda_auth, duda_user, etc.）
3. ✅ 启动服务测试

---

## 📞 需要帮助？

如果遇到问题，查看：

- `docs/Nacos配置创建指南.md` - Nacos 配置详细说明
- `docs/配置文件部署指南.md` - 完整部署步骤
- `docs/认证配置快速参考.md` - 认证信息汇总

---

**创建时间**: 2026-03-10
**状态**: ✅ 本地配置已完成，等待创建 Nacos 配置
